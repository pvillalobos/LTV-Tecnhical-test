package main

import (
	"fmt"
	"testing"
)

func test_ProcessReleasesRequest(t *testing.T) {
	total := 0
	//songController.ProcessReleasesRequest(ctx, time.Time.Now(), time.Time.Now)
	fmt.Println(t, total)
	//assert.NotNil(t,total,msgAndArgs: "asd")
}
